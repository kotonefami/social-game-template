[package]
name = "types"
version.workspace = true
edition.workspace = true
build = "build.rs"

[dependencies]
logic = { version = "0.1.0", path = "../logic" }
serde = { version = "1.0.228", features = ["derive"] }
ts-rs = { version = "12.0.1", features = ["chrono-impl"] }

[lib]
crate-type = ["rlib"]

